import 'package:flutter/material.dart';

class Chapter5Page extends StatelessWidget {
  const Chapter5Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('第五章 生僻语法'),
      ),
      body: const Center(
        child: Text('探索 Dart 语言中的高级语法特性'),
      ),
    );
  }
}
